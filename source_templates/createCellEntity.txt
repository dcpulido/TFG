if (entity.equalsIgnoreCase("$name")) {
            $name nentity=getOM().$createname(getMJGraph().getNewId("$name"));
            DefaultGraphCell vertex = new
            $namecell(nentity);
            // Default Size for the cell with the new entity
            return vertex;
        }
        else

        